# Lesson 7-2: PHPUnit と Laravel のテスト基礎 🐘
## 〜実際にテストを動かしながら学ぼう〜

---

## 🎭 プロローグ：今日のゴール

**👩‍💻ユーザー：** 「ガネーシャさん、前回でテストの大切さは分かりました！」

**🐘ガネーシャ：** 「おお、ええ心がけやな」

**👩‍💻ユーザー：** 「でも実際にどうやって書くのか、まだ分からなくて...」

**🐘ガネーシャ：** 「ほな今日は、**実際にテストを動かしながら**基礎を学んでいくで！」

**👩‍💻ユーザー：** 「動かしながら...！」

**🐘ガネーシャ：** 「せや。座学ばっかりやと眠くなるやろ？今日のゴールはこれや」

```
┌─────────────────────────────────────────────────────────────┐
│                  🎯 今日のゴール                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1️⃣  テスト用の簡単な API を作る                            │
│                                                             │
│  2️⃣  その API のテストを書く                                │
│                                                             │
│  3️⃣  テストを実行して「緑色で PASS」を見る！🎉              │
│                                                             │
│  → 実際に手を動かしながら、テストの基礎を学ぶで！           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「楽しそう！早くやりたいです！」

**🐘ガネーシャ：** 「ええ意気込みや。ほな早速始めるで！」

---

## 📖 第1章：まずはテスト対象の API を作ろう

### 🎭 テストするには「テスト対象」が必要

**🐘ガネーシャ：** 「テストを書くには、まず**テストする対象**が必要やろ？」

**👩‍💻ユーザー：** 「確かに、何をテストするかが決まってないと始まらないですね」

**🐘ガネーシャ：** 「せや。ほな、テスト練習用の**超シンプルな API**を作るで」

---

### 📝 ステップ1：Controller を作成

**🐘ガネーシャ：** 「ターミナルで以下のコマンドを実行してな」

```bash
# Controller を作成
sail artisan make:controller Api/TestController

```

**👩‍💻ユーザー：** 「実行しました！`app/Http/Controllers/Api/TestController.php` が作られました！」

---

### 📝 ステップ2：Controller にメソッドを追加

**🐘ガネーシャ：** 「作成されたファイルを開いて、以下の内容に**置き換え**てな」

```php
<?php
// app/Http/Controllers/Api/TestController.php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\JsonResponse;

class TestController extends Controller
{
    /**
     * テスト用：シンプルなレスポンスを返す
     */
    public function ping(): JsonResponse
    {
        return response()->json([
            'message' => 'pong',
            'status' => 'ok',
        ]);
    }

    /**
     * テスト用：受け取ったデータをそのまま返す
     */
    public function echo(): JsonResponse
    {
        return response()->json([
            'received' => request()->all(),
        ]);
    }
}
```

**👩‍💻ユーザー：** 「保存しました！」

**🐘ガネーシャ：** 「ええな。この API は超シンプルや」

```
┌─────────────────────────────────────────────────────────────┐
│                  今回作った API                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ping()  → 固定のレスポンスを返す                          │
│           { "message": "pong", "status": "ok" }             │
│                                                             │
│  echo()  → 送ったデータをそのまま返す                      │
│           { "received": { 送ったデータ } }                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 📝 ステップ3：ルートを追加

**🐘ガネーシャ：** 「次に、`routes/api.php` の**末尾に**以下を追加するで」

```php
// routes/api.php の末尾に追加

use App\Http\Controllers\Api\TestController;

// テスト練習用（認証なし）
Route::get('/test/ping', [TestController::class, 'ping']);
Route::post('/test/echo', [TestController::class, 'echo']);
```

**👩‍💻ユーザー：** 「追加しました！これで API が動くんですね」

**🐘ガネーシャ：** 「せや！認証なしのシンプルな API や。テスト練習にピッタリやで」

---

### 📝 ステップ4：Postman で動作確認

**🐘ガネーシャ：** 「API ができたら、まず Postman で動作確認するのが今までのやり方やったな？」

**👩‍💻ユーザー：** 「はい、やってみます！」

```
┌─────────────────────────────────────────────────────────────┐
│           Postman で確認（今までのやり方）                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1️⃣  Postman を起動                                         │
│  2️⃣  GET http://localhost/api/test/ping を送信             │
│  3️⃣  レスポンスを目で確認：                                 │
│      {                                                      │
│        "message": "pong",                                   │
│        "status": "ok"                                       │
│      }                                                      │
│  4️⃣  「うん、ちゃんと動いてるな...」                        │
│                                                             │
│  ⏱️  所要時間：約1〜2分                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「動きました！」

**🐘ガネーシャ：** 「よし、ほな**同じ確認をテストコードで自動化**してみよか！」

---

## 📖 第2章：テストファイルを作成しよう

### 🎭 テストファイルの作り方

**🐘ガネーシャ：** 「Laravel でテストファイルを作るには、コマンドを使うんや」

```bash
# テストファイルを作成
sail artisan make:test Api/TestControllerTest

```

**👩‍💻ユーザー：** 「実行しました！`tests/Feature/Api/TestControllerTest.php` が作られました！」

---

### 📝 作成されたファイルの中身

**🐘ガネーシャ：** 「作成されたファイルを見てみ」

```php
<?php

namespace Tests\Feature\Api;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Foundation\Testing\WithFaker;
use Tests\TestCase;

class TestControllerTest extends TestCase
{
    /**
     * A basic feature test example.
     */
    public function test_example(): void
    {
        $response = $this->get('/');

        $response->assertStatus(200);
    }
}
```

**👩‍💻ユーザー：** 「なんかサンプルコードが入ってますね」

**🐘ガネーシャ：** 「せや。これをベースに書き換えていくんやけど、その前に**ファイルの構造**を説明するで」

---

### 📊 テストファイルの構造

```
┌─────────────────────────────────────────────────────────────┐
│              テストファイルの構造                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  namespace Tests\Feature\Api;                               │
│  └─ テストファイルの場所を示す                              │
│     tests/Feature/Api/ フォルダに対応                       │
│                                                             │
│  class TestControllerTest extends TestCase                  │
│  └─ テストクラス名（〇〇Test という命名規則）               │
│     TestCase を継承すると Laravel のテスト機能が使える      │
│                                                             │
│  public function test_example(): void                       │
│  └─ テストメソッド                                          │
│     ⚠️ test_ で始めないと PHPUnit が認識しない！            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「`test_` で始めるのがポイントなんですね！」

**🐘ガネーシャ：** 「せや。`test_` で始まるメソッドだけがテストとして実行されるんや」

---

### 💡 補足：テストメソッド名を日本語にできる！

**👩‍💻ユーザー：** 「でも、`test_example` みたいな英語の名前だと、何をテストしているか分かりにくくないですか？」

**🐘ガネーシャ：** 「ええところに気づいたな！実は、PHPUnit では**テストメソッド名を日本語にできる**んや」

**👩‍💻ユーザー：** 「えっ、日本語でいいんですか！？」

**🐘ガネーシャ：** 「せや。2つの方法があるで」

```
┌─────────────────────────────────────────────────────────────┐
│          テストメソッド名を日本語にする方法                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  方法1️⃣：test_ プレフィックス + 日本語                       │
│                                                             │
│    public function test_Pingエンドポイントが正常に動作する(): void
│    {                                                        │
│        // テストコード                                      │
│    }                                                        │
│                                                             │
│  方法2️⃣：#[Test] 属性 + 日本語メソッド名                     │
│                                                             │
│    #[Test]                                                  │
│    public function Pingエンドポイントが正常に動作する(): void  │
│    {                                                        │
│        // テストコード                                      │
│    }                                                        │
│                                                             │
│  ⚠️ どちらの方法でも、メソッド名は public にすること！       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「日本語で書けると、一目で何のテストか分かりますね！」

**🐘ガネーシャ：** 「せやろ？特に方法2️⃣の `#[Test]` 属性を使う方法は、`test_` プレフィックスが不要やから、より自然な日本語で書けるんや」

```php
<?php
// 例：日本語メソッド名の実例

namespace Tests\Feature\Api;

use Tests\TestCase;
use PHPUnit\Framework\Attributes\Test;

class TestControllerTest extends TestCase
{
    // 方法1：test_ プレフィックス + 日本語
    public function test_Pingエンドポイントが正常に動作する(): void
    {
        $response = $this->get('/api/test/ping');
        $response->assertStatus(200);
    }

    // 方法2：#[Test] 属性 + 日本語メソッド名
    #[Test]
    public function Echoエンドポイントが送信したデータを返す(): void
    {
        $response = $this->post('/api/test/echo', ['name' => 'test']);
        $response->assertStatus(200);
    }
}
```

**🐘ガネーシャ：** 「テストが失敗した時も、どのテストが落ちたか日本語で表示されるから、めっちゃ分かりやすいで」

```bash
# テスト実行結果の例
FAILED  Tests\Feature\Api\TestControllerTest > Echoエンドポイントが送信したデータを返す
```

**👩‍💻ユーザー：** 「これなら、後から見返した時も何をテストしているか一目瞭然ですね！」

**🐘ガネーシャ：** 「せや。ただし、**プロジェクトのコーディング規約**によっては英語で統一している場合もあるから、チームのルールに従ってな」

```
┌─────────────────────────────────────────────────────────────┐
│          日本語メソッド名を使うメリット                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ✅ 何をテストしているか一目で分かる                        │
│  ✅ テスト失敗時のエラーメッセージが分かりやすい            │
│  ✅ 仕様書のような役割も果たせる                            │
│  ✅ チーム内でのコミュニケーションがスムーズ                │
│                                                             │
│  ⚠️ 注意点                                                  │
│  ・プロジェクトのコーディング規約を確認                    │
│  ・英語統一のプロジェクトでは使わない                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「分かりました！このプロジェクトでは日本語を使ってもいいんですか？」

**🐘ガネーシャ：** 「このプロジェクトは学習用やから、**どっちでも好きな方を使ってええで**。今回は最初なので、まずは基本の `test_` プレフィックスで進めて、慣れてきたら日本語も試してみるとええで」

**👩‍💻ユーザー：** 「了解です！」

---

## 📖 第3章：最初のテストを書こう

### 🎭 ping API のテストを書く

**🐘ガネーシャ：** 「ほな、`test_example` を削除して、ping API のテストを書いてみよか」

**👩‍💻ユーザー：** 「はい！」

**🐘ガネーシャ：** 「ファイルを以下の内容に**置き換え**てな」

```php
<?php
// tests/Feature/Api/TestControllerTest.php

namespace Tests\Feature\Api;

use Tests\TestCase;

class TestControllerTest extends TestCase
{
    /**
     * /api/test/ping が正しいレスポンスを返す
     */
    public function test_ping_returns_pong(): void
    {
        // API にリクエストを送る
        $response = $this->getJson('/api/test/ping');

        // ステータスコードが 200 か確認
        $response->assertStatus(200);

        // JSON の中身が正しいか確認
        $response->assertJson([
            'message' => 'pong',
            'status' => 'ok',
        ]);
    }
}
```

**👩‍💻ユーザー：** 「保存しました！」

---

### 🤔 コードの解説

**🐘ガネーシャ：** 「1行ずつ解説するで」

**👩‍💻ユーザー：** 「お願いします！」

---

#### 🔍 getJson() - リクエストを送る

```php
$response = $this->getJson('/api/test/ping');
```

**🐘ガネーシャ：** 「この1行で、**実際に API が動く**んやで」

**👩‍💻ユーザー：** 「え、本当に動くんですか？」

**🐘ガネーシャ：** 「せや！裏で何が起きてるか図で見せたるわ」

```
┌─────────────────────────────────────────────────────────────┐
│        getJson() を実行すると裏で何が起きる？               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  $this->getJson('/api/test/ping');                          │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 1️⃣  Laravel が /api/test/ping への      │                │
│  │    GET リクエストを受け取ったフリをする │                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 2️⃣  routes/api.php を見て               │                │
│  │    「あ、TestController の ping() やな」│                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 3️⃣  TestController の ping() を実行     │                │
│  │    return response()->json([...]);      │                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 4️⃣  レスポンスが返ってくる              │                │
│  │    {"message":"pong","status":"ok"}     │                │
│  │    ステータスコード: 200                │                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  $response に結果が入る！                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「えっ、本当に Controller が動いてるんですか！？」

**🐘ガネーシャ：** 「せや！**テストなのに本番と同じコードが動く**んや。だからバグがあったらテストで見つけられるんやで」

**👩‍💻ユーザー：** 「Postman で Send ボタンを押すのと同じことが起きてるんですね...！」

**🐘ガネーシャ：** 「その通りや！ただ、ブラウザや Postman を使わずに、**コードから直接リクエストを送ってる**だけの違いや」

---

#### 🔍 assertStatus() - ステータスコードを確認

```php
$response->assertStatus(200);
```

**🐘ガネーシャ：** 「これは『さっきのリクエストが成功したか？』を確認しとるんや」

**👩‍💻ユーザー：** 「ちょっと待ってください！」

**🐘ガネーシャ：** 「ん？どないした？」

**👩‍💻ユーザー：** 「Controller のコードを見ると、`return response()->json([...])` だけで、**200 なんてどこにも書いてない**ですよね？なんで 200 が返ってくるんですか？」

**🐘ガネーシャ：** 「おお、ええとこに気づいたな！実はな...」

```
┌─────────────────────────────────────────────────────────────┐
│          「200 ってどこから来るの？」問題                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【Controller のコード】                                    │
│                                                             │
│  return response()->json([                                  │
│      'message' => 'pong',                                   │
│      'status' => 'ok',                                      │
│  ]);                                                        │
│                                                             │
│  → 確かに 200 とは書いてない！🤔                           │
│                                                             │
│  ─────────────────────────────────────────────────          │
│                                                             │
│  【答え】Laravel が自動で 200 を付けてくれる！              │
│                                                             │
│  response()->json() は、特に指定しなければ                  │
│  **「成功したよ」という意味の 200** を自動で返すんや        │
│                                                             │
│  もし 201（作成成功）を返したい場合は明示的に書く：         │
│  return response()->json([...], 201);                       │
│                      ここで指定 ──┘                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「なるほど！書かなければ自動で 200 になるんですね」

**🐘ガネーシャ：** 「せや！逆に言えば、**エラーが起きたら 200 以外**が返ってくるんや」

---

#### 💥 もしエラーが起きたら？

**👩‍💻ユーザー：** 「エラーが起きたらどうなるんですか？」

**🐘ガネーシャ：** 「ええ質問や。Controller でエラーが起きたら、Laravel は自動で**500番（サーバーエラー）**を返すんや」

```
┌─────────────────────────────────────────────────────────────┐
│              エラーが起きた時の流れ                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【もし Controller にバグがあったら...】                    │
│                                                             │
│  public function ping(): JsonResponse                       │
│  {                                                          │
│      $undefined->method();  // ← 存在しない変数を使用！💥   │
│                                                             │
│      return response()->json([                              │
│          'message' => 'pong',                               │
│          'status' => 'ok',                                  │
│      ]);                                                    │
│  }                                                          │
│                                                             │
│  ─────────────────────────────────────────────────          │
│                                                             │
│  【結果】                                                   │
│                                                             │
│  ❌ エラーが発生！                                          │
│  ❌ return まで到達しない                                   │
│  ❌ Laravel が 500 Internal Server Error を返す             │
│                                                             │
│  → テストで assertStatus(200) してたら「失敗」になる！     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「つまり、assertStatus(200) は『エラーなく正常に処理が終わったか』を確認してるんですね！」

**🐘ガネーシャ：** 「その通りや！賢いな〜。さすがワシの弟子や」

**👩‍💻ユーザー：** 「（自分で言うんだ...）」

---

#### 📊 ステータスコードの意味（よく使うもの）

**🐘ガネーシャ：** 「ついでに、よく使うステータスコードを教えとくわ」

```
┌─────────────────────────────────────────────────────────────┐
│              よく使うステータスコード                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【成功系】                                                 │
│  200 OK          → 成功したで！（一番よく使う）            │
│  201 Created     → 新しく作ったで！（POST で作成時）       │
│  204 No Content  → 成功したけど返すものないで（DELETE時）  │
│                                                             │
│  【クライアントエラー系】（リクエストが悪い）               │
│  400 Bad Request → リクエストがおかしいで                  │
│  401 Unauthorized→ ログインしてへんで                      │
│  403 Forbidden   → 権限ないで                              │
│  404 Not Found   → そんなもん見つからへんで                │
│  422 Unprocessable → バリデーションエラーやで              │
│                                                             │
│  【サーバーエラー系】（サーバー側の問題）                   │
│  500 Internal Server Error → サーバーでエラー起きたで💥    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「500 が出たら、Controller にバグがある可能性が高いんですね」

**🐘ガネーシャ：** 「せや！テストで 500 が出たら『あ、コードにバグあるな』ってすぐ分かるんや」

---

#### 📊 assertStatus(200) の意味 まとめ

```
┌─────────────────────────────────────────────────────────────┐
│              assertStatus(200) の意味 まとめ                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  「$response に入ってるステータスコードが 200 か確認してな」│
│                                                             │
│  ✅ 200 だった場合 → テスト成功！次の行へ進む              │
│  ❌ 200 以外の場合 → テスト失敗！ここで止まる              │
│                                                             │
│  ─────────────────────────────────────────────────          │
│                                                             │
│  【何を確認してる？】                                       │
│                                                             │
│  ・Controller がエラーなく最後まで実行されたか              │
│  ・return response()->json() まで到達したか                 │
│  ・途中で例外が発生していないか                             │
│                                                             │
│  ─────────────────────────────────────────────────          │
│                                                             │
│  【Postman との比較】                                       │
│                                                             │
│  Postman: レスポンスの上に「200 OK」と表示される            │
│           → 「よし、200 やな」と自分の目で確認 👀          │
│                                                             │
│  テスト:  assertStatus(200) と書いておく                    │
│           → 勝手に確認してくれる！🤖                       │
│           → 違ったら「200 のはずが 500 やったで！」と教えてくれる│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「目で見て確認してたことを、コードが代わりにやってくれるんですね！」

---

#### 🔍 assertJson() - JSON の中身を確認

```php
$response->assertJson([
    'message' => 'pong',
    'status' => 'ok',
]);
```

**🐘ガネーシャ：** 「これは『返ってきた JSON の中身が正しいか？』を確認しとるんや」

```
┌─────────────────────────────────────────────────────────────┐
│              assertJson() の意味                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  「$response の JSON に、この内容が含まれてるか確認してな」 │
│                                                             │
│  期待する値（テストに書いた）:                              │
│    {                                                        │
│      "message": "pong",                                     │
│      "status": "ok"                                         │
│    }                                                        │
│                                                             │
│  実際に Controller から返ってきた値:                        │
│    {                                                        │
│      "message": "pong",                                     │
│      "status": "ok"                                         │
│    }                                                        │
│                                                             │
│  → 一致してるから ✅ テスト成功！                          │
│                                                             │
│  ─────────────────────────────────────────────────          │
│                                                             │
│  【Postman との比較】                                       │
│                                                             │
│  Postman: JSON を目で見て「message が pong で...」と確認 👀 │
│                                                             │
│  テスト:  assertJson() で自動確認 🤖                        │
│           → 違ったら「pong のはずが ping やったで！」と     │
│             教えてくれる                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「なるほど...！Controller が返す値と、テストで期待する値を比較してるんですね」

**🐘ガネーシャ：** 「せや！もし Controller のコードを誰かが間違えて変更したら...」

```php
// もし誰かがこう書き換えたら...
return response()->json([
    'message' => 'hello',  // ← pong じゃなくなった！
    'status' => 'ok',
]);
```

**🐘ガネーシャ：** 「テストを実行した瞬間に『pong のはずが hello やったで！』ってエラーになるんや」

**👩‍💻ユーザー：** 「だからバグを早く見つけられるんですね...！」

---

### 📊 テストの流れ まとめ

**🐘ガネーシャ：** 「ここまでの流れを整理するで」

```
┌─────────────────────────────────────────────────────────────┐
│                 テスト実行の全体像                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  テストコード                                               │
│  ────────────────────────────────────────────               │
│                                                             │
│  $response = $this->getJson('/api/test/ping');              │
│       │                                                     │
│       │  ① リクエスト送信                                  │
│       ▼                                                     │
│  ┌─────────────┐     ┌─────────────────────┐                │
│  │   routes/   │ ──▶ │  TestController     │                │
│  │   api.php   │     │  ping() メソッド    │                │
│  └─────────────┘     └──────────┬──────────┘                │
│                                 │                           │
│                                 │ ② 処理実行 & レスポンス  │
│                                 ▼                           │
│                      {"message":"pong","status":"ok"}       │
│                      ステータスコード: 200                  │
│                                 │                           │
│       ┌─────────────────────────┘                           │
│       │  ③ 結果が $response に入る                         │
│       ▼                                                     │
│  $response->assertStatus(200);        ④ 200か確認 → ✅     │
│  $response->assertJson([...]);        ⑤ JSON確認 → ✅      │
│                                                             │
│  → すべて OK なら「テスト成功！」🎉                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「おお！全体の流れが見えました！」

**🐘ガネーシャ：** 「せやろ？**テストは本物のコードを動かして確認してる**んや。だから信頼できるんやで」

**👩‍💻ユーザー：** 「Postman でやってたことが全部コードになってる！」

**🐘ガネーシャ：** 「せやろ？ほな、実行してみよか！」

---

## 📖 第4章：テストを実行しよう！

### 🎭 テスト実行コマンド

**🐘ガネーシャ：** 「テストを実行するコマンドはこれや」

```bash
# 特定のテストファイルだけ実行
sail artisan test --filter=TestControllerTest

```

**👩‍💻ユーザー：** 「ドキドキ...実行します！」

---

### 🎉 テスト結果を見る

```
   PASS  Tests\Feature\Api\TestControllerTest
  ✓ ping returns pong                                        0.05s

  Tests:    1 passed (2 assertions)
  Duration: 0.12s
```

**👩‍💻ユーザー：** 「通った！！緑色！！🎉」

**🐘ガネーシャ：** 「おめでとう！**人生初の自動テストが成功**したな！」

---

### 📊 結果の見方

**🐘ガネーシャ：** 「テスト結果の見方を説明するで」

```
┌─────────────────────────────────────────────────────────────┐
│                  テスト結果の見方                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   PASS  Tests\Feature\Api\TestControllerTest                │
│   └─ このテストファイルが PASS（成功）したで                │
│                                                             │
│   ✓ ping returns pong                            0.05s      │
│   └─ test_ping_returns_pong メソッドが成功                  │
│      0.05秒で実行完了                                       │
│                                                             │
│   Tests:    1 passed (2 assertions)                         │
│   └─ 1つのテストが成功、2つの検証（assert）を実行          │
│      assertStatus と assertJson の2つ                       │
│                                                             │
│   Duration: 0.12s                                           │
│   └─ 全体で0.12秒で完了                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「0.12秒！Postman だと1分くらいかかってたのに！」

**🐘ガネーシャ：** 「せや！**約500倍速い**んや。しかも自動やから、お前は見てるだけでええ」

---

## 📖 第5章：テストの基本パターン（AAA パターン）

### 🎭 テストには決まった書き方がある

**🐘ガネーシャ：** 「さっき書いたテスト、実はある**パターン**に沿っとるんや」

**👩‍💻ユーザー：** 「パターン？」

**🐘ガネーシャ：** 「**AAA パターン**っていうんや。覚えときや」

```
┌─────────────────────────────────────────────────────────────┐
│                   AAA パターン                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1️⃣  Arrange（準備）                                        │
│      → テストに必要なデータを用意する                       │
│                                                             │
│  2️⃣  Act（実行）                                            │
│      → テスト対象の処理を実行する                           │
│                                                             │
│  3️⃣  Assert（検証）                                         │
│      → 結果が期待通りか確認する                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 📝 さっきのテストを AAA で見てみる

**🐘ガネーシャ：** 「さっき書いたテストを AAA パターンで見てみよか」

```php
public function test_ping_returns_pong(): void
{
    // ============================================
    // 1. Arrange（準備）
    // ============================================
    // → 今回は認証不要なので、特に準備なし！

    // ============================================
    // 2. Act（実行）
    // ============================================
    $response = $this->getJson('/api/test/ping');

    // ============================================
    // 3. Assert（検証）
    // ============================================
    $response->assertStatus(200);
    $response->assertJson([
        'message' => 'pong',
        'status' => 'ok',
    ]);
}
```

**👩‍💻ユーザー：** 「準備 → 実行 → 検証 の流れですね！」

**🐘ガネーシャ：** 「せや！この流れを覚えておけば、どんなテストも書けるようになるで」

---

### 🍳 料理で例える AAA パターン

**🐘ガネーシャ：** 「料理で例えると分かりやすいで」

```
┌─────────────────────────────────────────────────────────────┐
│                🍳 料理で例える AAA パターン                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【作りたい料理】目玉焼き                                   │
│                                                             │
│  1️⃣  Arrange（準備）                                        │
│      → 卵を用意する                                         │
│      → フライパンを温める                                   │
│      → 油をひく                                             │
│                                                             │
│  2️⃣  Act（実行）                                            │
│      → 卵を割って焼く                                       │
│                                                             │
│  3️⃣  Assert（検証）                                         │
│      → 白身は固まってるか？ ✅                              │
│      → 黄身は半熟か？ ✅                                    │
│      → 焦げてないか？ ✅                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「なるほど！テストも料理も同じ流れなんですね」

**🐘ガネーシャ：** 「せや！どんなテストもこの3ステップで書けるんや」

---

## 📖 第6章：もう1つテストを追加しよう

### 🎭 echo API のテストを追加

**🐘ガネーシャ：** 「今度は echo API のテストを追加してみよか。AAA パターンを意識してな」

**👩‍💻ユーザー：** 「はい！」

**🐘ガネーシャ：** 「`test_ping_returns_pong` の下に、新しいテストメソッドを追加するで」

```php
<?php
// tests/Feature/Api/TestControllerTest.php

namespace Tests\Feature\Api;

use Tests\TestCase;

class TestControllerTest extends TestCase
{
    /**
     * /api/test/ping が正しいレスポンスを返す
     */
    public function test_ping_returns_pong(): void
    {
        // 1. Arrange（準備）
        // → 認証不要なので、特になし

        // 2. Act（実行）
        $response = $this->getJson('/api/test/ping');

        // 3. Assert（検証）
        $response->assertStatus(200);
        $response->assertJson([
            'message' => 'pong',
            'status' => 'ok',
        ]);
    }

    /**
     * /api/test/echo が送ったデータをそのまま返す
     */
    public function test_echo_returns_received_data(): void
    {
        // ============================================
        // 1. Arrange（準備）
        // ============================================
        $sendData = [
            'name' => 'テスト太郎',
            'age' => 25,
        ];

        // ============================================
        // 2. Act（実行）
        // ============================================
        $response = $this->postJson('/api/test/echo', $sendData);

        // ============================================
        // 3. Assert（検証）
        // ============================================
        $response->assertStatus(200);
        $response->assertJson([
            'received' => [
                'name' => 'テスト太郎',
                'age' => 25,
            ],
        ]);
    }
}
```

**👩‍💻ユーザー：** 「今度は Arrange で送るデータを準備してますね！」

**🐘ガネーシャ：** 「そこに気づいたか！ええな。今度は POST リクエストやから、送るデータが必要なんや」

---

### 📊 GET と POST の違い

```
┌─────────────────────────────────────────────────────────────┐
│                  GET と POST の違い                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【GET リクエスト】                                         │
│  $this->getJson('/api/test/ping');                          │
│  → データを送らずにリクエスト                              │
│  → Postman で「GET を選んで Send」と同じ                   │
│                                                             │
│  【POST リクエスト】                                        │
│  $this->postJson('/api/test/echo', $sendData);              │
│  → データを送ってリクエスト                                │
│  → Postman で「POST を選んで Body に JSON 入れて Send」と同じ│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「なるほど、POST はデータを一緒に送るんですね」

**🐘ガネーシャ：** 「せや！ほな、echo テストのコードを1行ずつ解説するで」

---

### 🤔 echo テストのコード解説

#### 🔍 Arrange（準備）- 送るデータを用意

```php
$sendData = [
    'name' => 'テスト太郎',
    'age' => 25,
];
```

**🐘ガネーシャ：** 「まず、API に送るデータを準備するんや」

**👩‍💻ユーザー：** 「Postman でいうと、Body に入力する JSON のことですか？」

**🐘ガネーシャ：** 「その通りや！」

```
┌─────────────────────────────────────────────────────────────┐
│              Arrange（準備）の意味                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【Postman の場合】                                         │
│                                                             │
│  1. Body タブをクリック                                     │
│  2. raw を選択                                              │
│  3. JSON を選択                                             │
│  4. 以下を入力：                                            │
│     {                                                       │
│       "name": "テスト太郎",                                 │
│       "age": 25                                             │
│     }                                                       │
│                                                             │
│  【テストコードの場合】                                     │
│                                                             │
│  $sendData = [                                              │
│      'name' => 'テスト太郎',                                │
│      'age' => 25,                                           │
│  ];                                                         │
│                                                             │
│  → PHP の配列で書くだけ！JSON に自動変換してくれる         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「Postman だと4ステップかかるのに、コードだと3行で終わる...！」

---

#### 🔍 Act（実行）- POST リクエストを送る

```php
$response = $this->postJson('/api/test/echo', $sendData);
```

**🐘ガネーシャ：** 「この1行で、**データを送りながら API を叩く**んや」

**👩‍💻ユーザー：** 「`getJson` と違って、第2引数にデータを渡すんですね！」

**🐘ガネーシャ：** 「せや！裏で何が起きてるか見せたるわ」

```
┌─────────────────────────────────────────────────────────────┐
│        postJson() を実行すると裏で何が起きる？              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  $this->postJson('/api/test/echo', $sendData);              │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 1️⃣  Laravel が /api/test/echo への      │                │
│  │    POST リクエストを受け取ったフリをする│                │
│  │                                         │                │
│  │    📦 送信データ:                        │                │
│  │    {"name":"テスト太郎","age":25}        │                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 2️⃣  routes/api.php を見て               │                │
│  │    「あ、TestController の echo() やな」│                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 3️⃣  TestController の echo() を実行     │                │
│  │                                         │                │
│  │    public function echo()               │                │
│  │    {                                    │                │
│  │        // request()->all() で           │                │
│  │        // 送られてきたデータを取得      │                │
│  │        return response()->json([        │                │
│  │            'received' => request()->all()│               │
│  │        ]);                              │                │
│  │    }                                    │                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  ┌─────────────────────────────────────────┐                │
│  │ 4️⃣  レスポンスが返ってくる              │                │
│  │                                         │                │
│  │    {                                    │                │
│  │      "received": {                      │                │
│  │        "name": "テスト太郎",            │                │
│  │        "age": 25                        │                │
│  │      }                                  │                │
│  │    }                                    │                │
│  │    ステータスコード: 200                │                │
│  └─────────────────────────────────────────┘                │
│          │                                                  │
│          ▼                                                  │
│  $response に結果が入る！                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「`request()->all()` で送ったデータを受け取って、それをそのまま返してるんですね！」

**🐘ガネーシャ：** 「せや！echo（こだま）っていう名前の通り、送ったものがそのまま返ってくるんや」

---

#### 🔍 Assert（検証）- 返ってきたデータを確認

```php
$response->assertStatus(200);
$response->assertJson([
    'received' => [
        'name' => 'テスト太郎',
        'age' => 25,
    ],
]);
```

**👩‍💻ユーザー：** 「ここで『送ったデータがちゃんと返ってきてるか』を確認するんですね！」

**🐘ガネーシャ：** 「せや！」

```
┌─────────────────────────────────────────────────────────────┐
│              Assert（検証）の意味                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【確認していること】                                       │
│                                                             │
│  assertStatus(200)                                          │
│  └─ Controller がエラーなく処理を完了したか？               │
│                                                             │
│  assertJson([...])                                          │
│  └─ 送ったデータが、ちゃんと received の中に入って          │
│     返ってきてるか？                                        │
│                                                             │
│  ─────────────────────────────────────────────────          │
│                                                             │
│  【期待する値】           【実際に返ってきた値】            │
│                                                             │
│  {                        {                                 │
│    "received": {            "received": {                   │
│      "name": "テスト太郎",    "name": "テスト太郎",         │
│      "age": 25                "age": 25                     │
│    }                        }                               │
│  }                        }                                 │
│                                                             │
│  → 一致してるから ✅ テスト成功！                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

#### 💥 もし Controller にバグがあったら？

**👩‍💻ユーザー：** 「echo も、エラーになったらどうなるんですか？」

**🐘ガネーシャ：** 「ええ質問や。例えば、Controller をこう間違えたとするやろ？」

```php
// 間違ったコード
public function echo(): JsonResponse
{
    return response()->json([
        'received' => $request->all(),  // ← $request が定義されてない！💥
    ]);
}
```

**🐘ガネーシャ：** 「`$request` を定義し忘れたら、エラーになるんや」

```
┌─────────────────────────────────────────────────────────────┐
│              バグがあった時のテスト結果                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   FAIL  Tests\Feature\Api\TestControllerTest                │
│  ✕ echo returns received data                   0.08s       │
│  ┐                                                          │
│  │ Expected status code 200 but received 500.               │
│  │                                                          │
│  │ at tests/Feature/Api/TestControllerTest.php:35           │
│  ┴                                                          │
│                                                             │
│  → 「200 を期待してたけど、500 が返ってきたで！」          │
│  → つまり Controller でエラーが起きてる！                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「500 が返ってきたら『Controller にバグがある』ってすぐ分かるんですね！」

**🐘ガネーシャ：** 「せや！これがテストの力や。**バグを自動で見つけてくれる**んやで」

---

#### 💥 もしデータの加工を間違えたら？

**🐘ガネーシャ：** 「もう1つ例を見せたるわ。エラーにはならへんけど、結果が違う場合や」

```php
// 間違ったコード（エラーにはならない）
public function echo(): JsonResponse
{
    return response()->json([
        'data' => request()->all(),  // ← 'received' じゃなくて 'data' にしてしまった！
    ]);
}
```

**🐘ガネーシャ：** 「この場合、500 エラーにはならへん。でも...」

```
┌─────────────────────────────────────────────────────────────┐
│              キーを間違えた時のテスト結果                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   FAIL  Tests\Feature\Api\TestControllerTest                │
│  ✕ echo returns received data                   0.06s       │
│  ┐                                                          │
│  │ Unable to find JSON:                                     │
│  │                                                          │
│  │ Expected: {"received":{"name":"テスト太郎","age":25}}    │
│  │ Actual:   {"data":{"name":"テスト太郎","age":25}}        │
│  │                                                          │
│  │ at tests/Feature/Api/TestControllerTest.php:38           │
│  ┴                                                          │
│                                                             │
│  → 「received を期待してたけど、実際は data やったで！」   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「おお！キー名が違うのもちゃんと検出してくれる！」

**🐘ガネーシャ：** 「せや！**Expected（期待値）**と**Actual（実際の値）**を比較して、違いを教えてくれるんや」

**👩‍💻ユーザー：** 「これなら、API の仕様が変わってもすぐ気づけますね！」

**🐘ガネーシャ：** 「その通りや！誰かが間違えてコードを変更しても、テストが『おい、仕様と違うで！』って教えてくれるんや。さすがワシの弟子やな」

---

### 🎭 テストを実行

**🐘ガネーシャ：** 「ほな、正しいコードに戻して実行してみよか」

```bash
sail artisan test --filter=TestControllerTest
```

```
   PASS  Tests\Feature\Api\TestControllerTest
  ✓ ping returns pong                                        0.05s
  ✓ echo returns received data                               0.03s

  Tests:    2 passed (4 assertions)
  Duration: 0.15s
```

**👩‍💻ユーザー：** 「2つとも通った！」

**🐘ガネーシャ：** 「**0.15秒で2つのテストが完了**や。Postman で同じことやったら3〜4分はかかるやろ？」

---

## 📖 第7章：PHPUnit と Laravel の関係

### 🎭 PHPUnit って何？

**👩‍💻ユーザー：** 「そういえば、さっきから出てくる PHPUnit って何ですか？」

**🐘ガネーシャ：** 「ええ質問や。**PHPUnit は PHP のテストを書くためのツール**なんや」

```
┌─────────────────────────────────────────────────────────────┐
│                  PHPUnit とは                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🍳 料理で例えると...                                        │
│                                                             │
│    料理を作る        → コードを書く（PHP）                  │
│    キッチン用品      → テストツール（PHPUnit）              │
│    フライパン、包丁  → assertStatus、assertJson など        │
│                                                             │
│  キッチン用品がないと料理しづらいように、                   │
│  PHPUnit がないとテストが書きづらいんや！                   │
│                                                             │
│  ✅ Laravel には最初から PHPUnit が入ってる                 │
│  → インストール不要！すぐ使える！                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「Laravel には最初から入ってるんですね！」

**🐘ガネーシャ：** 「せや。だから `php artisan test` コマンドですぐテストが実行できるんや」

---

### 📊 Laravel プロジェクトのテスト構成

**🐘ガネーシャ：** 「Laravel のテストに関係するファイルを見てみよか」

```
┌─────────────────────────────────────────────────────────────┐
│               Laravel テストの構成                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   プロジェクト/                                              │
│   ├── app/           ← 本番コード                           │
│   │   └── Http/Controllers/Api/                             │
│   │       └── TestController.php  ← これをテストする        │
│   │                                                         │
│   ├── tests/         ← テストコードを置く場所               │
│   │   ├── Feature/   ← 機能テスト（API など）               │
│   │   │   └── Api/                                          │
│   │   │       └── TestControllerTest.php  ← テストコード    │
│   │   └── Unit/      ← 単体テスト                           │
│   │                                                         │
│   └── phpunit.xml    ← PHPUnit の設定ファイル               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「`tests/Feature/` にテストを置くんですね」

---

### 📊 Feature テストと Unit テストの違い

**🐘ガネーシャ：** 「テストには2種類あるんや」

```
┌─────────────────────────────────────────────────────────────┐
│                  テストの種類                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【Feature Test（機能テスト）】← 今回やってるやつ           │
│    → API 全体が正しく動くかテスト                          │
│    → 「リクエストを送ってレスポンスを確認」                │
│    → Postman でやってたことをコードにしたもの              │
│                                                             │
│  【Unit Test（単体テスト）】                                 │
│    → 1つの関数やメソッドだけをテスト                       │
│    → 「この計算ロジック、正しく動く？」                    │
│    → 部品単位のテスト                                      │
│                                                             │
│  🚗 車で例えると...                                          │
│    Feature Test → 車全体が走れるかテスト                   │
│    Unit Test    → エンジン単体が動くかテスト               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「今回は Feature Test をやってるんですね！」

**🐘ガネーシャ：** 「せや！API のテストは Feature Test で書くのが基本や」

---

## 📖 第8章：よく使うメソッドを覚えよう

### 🎭 HTTP リクエストを送るメソッド

**🐘ガネーシャ：** 「テストでよく使うメソッドをまとめて紹介するで」

```
┌─────────────────────────────────────────────────────────────┐
│             HTTPリクエストを送るメソッド                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Postman の操作              テストコード                   │
│  ──────────────────          ────────────────────────       │
│                                                             │
│  GET リクエスト              $this->getJson('/api/...')     │
│  POST リクエスト             $this->postJson('/api/...', [])│
│  PUT リクエスト              $this->putJson('/api/...', []) │
│  PATCH リクエスト            $this->patchJson('/api/',[])   │
│  DELETE リクエスト           $this->deleteJson('/api/...')  │
│                                                             │
│  ※ すべて JSON 形式でリクエストを送る                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「HTTP メソッドごとに対応するメソッドがあるんですね！」

---

### 📊 レスポンスを検証するメソッド

```
┌─────────────────────────────────────────────────────────────┐
│             レスポンスを検証するメソッド                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【ステータスコードの確認】                                 │
│  assertStatus(200)      → ステータスコードが 200 か        │
│  assertOk()             → 200 か（省略形）                 │
│  assertCreated()        → 201 か                           │
│  assertNoContent()      → 204 か                           │
│  assertNotFound()       → 404 か                           │
│  assertForbidden()      → 403 か                           │
│  assertUnauthorized()   → 401 か                           │
│                                                             │
│  【JSONの中身の確認】                                       │
│  assertJson([...])      → 指定した内容が含まれるか         │
│  assertExactJson([...]) → 完全に一致するか                 │
│  assertJsonCount(n)     → 配列の件数が n か                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「`assertOk()` は `assertStatus(200)` と同じなんですね！」

**🐘ガネーシャ：** 「せや。よく使うステータスコードには省略形があるんや」

---

## 📖 第9章：テストが失敗したらどうなる？

### 🎭 わざと失敗させてみよう

**🐘ガネーシャ：** 「テストが失敗した時どうなるか見てみよか」

**👩‍💻ユーザー：** 「え、わざと失敗させるんですか？」

**🐘ガネーシャ：** 「せや。失敗した時の見方を知っておくのは大事やで」

---

### 📝 期待値を間違えてみる

**🐘ガネーシャ：** 「`test_ping_returns_pong` の `assertJson` を間違えてみ」

```php
// わざと間違える
$response->assertJson([
    'message' => 'ping',  // ← pong じゃなくて ping にしてみる
    'status' => 'ok',
]);
```

**👩‍💻ユーザー：** 「実行してみます...」

```
   FAIL  Tests\Feature\Api\TestControllerTest
  ✕ ping returns pong                                        0.08s
  ┐
  │ Failed asserting that an array contains subset Array &0 (
  │     'message' => 'ping'
  │     'status' => 'ok'
  │ ).
  │ 
  │ Actual: {"message":"pong","status":"ok"}
  │ 
  │ at tests/Feature/Api/TestControllerTest.php:20
  ┴

  Tests:    1 failed, 1 passed
```

**👩‍💻ユーザー：** 「あ、赤くなった！」

---

### 📊 エラーメッセージの読み方

**🐘ガネーシャ：** 「エラーメッセージの読み方を教えるで」

```
┌─────────────────────────────────────────────────────────────┐
│                 エラーメッセージの読み方                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ✕ ping returns pong                                        │
│  └─ このテストが失敗したで                                  │
│                                                             │
│  Failed asserting that an array contains subset             │
│  └─ 「配列に含まれてるはずの内容がなかったで」             │
│                                                             │
│  'message' => 'ping'                                        │
│  └─ これを期待してた（テストコードに書いた値）             │
│                                                             │
│  Actual: {"message":"pong","status":"ok"}                   │
│  └─ 実際に返ってきた値                                     │
│                                                             │
│  at tests/Feature/Api/TestControllerTest.php:20             │
│  └─ 失敗した場所（20行目）                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**👩‍💻ユーザー：** 「なるほど！『ping を期待してたけど、実際は pong だった』って教えてくれてるんですね」

**🐘ガネーシャ：** 「せや！このメッセージを見れば、何が間違ってるか分かるんや。元に戻しといてな」

---

## 📖 第10章：テスト実行コマンドまとめ

### 🎭 よく使うコマンド

**🐘ガネーシャ：** 「最後に、テストを実行するコマンドをまとめとくで」

```bash
# ======================================
# すべてのテストを実行
# ======================================
sail artisan test

# ======================================
# 特定のテストファイルだけ実行
# ======================================
sail artisan test --filter=TestControllerTest

# ======================================
# 特定のテストメソッドだけ実行
# ======================================
sail artisan test --filter=test_ping_returns_pong

# ======================================
# 失敗したらすぐ止める（CI向け）
# ======================================
sail artisan test --stop-on-failure

# ======================================
# 詳細な出力
# ======================================
sail artisan test --verbose
```

**👩‍💻ユーザー：** 「`--filter` オプションが便利ですね！」

**🐘ガネーシャ：** 「せや。開発中は特定のテストだけ実行することが多いからな」

---

## 📝 まとめ

### 🎯 今日学んだこと

```
┌─────────────────────────────────────────────────────────────┐
│                    📝 Lesson 7-2 まとめ                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1️⃣  テスト対象の API を作った                              │
│      → TestController（ping, echo）                        │
│                                                             │
│  2️⃣  テストファイルを作成した                               │
│      → php artisan make:test Api/TestControllerTest        │
│                                                             │
│  3️⃣  テストを書いた                                         │
│      → getJson() でリクエスト                              │
│      → assertStatus(), assertJson() で検証                 │
│                                                             │
│  4️⃣  テストを実行して成功した！🎉                           │
│      → php artisan test --filter=TestControllerTest        │
│                                                             │
│  5️⃣  AAA パターンを学んだ                                   │
│      → Arrange（準備）→ Act（実行）→ Assert（検証）        │
│                                                             │
│  6️⃣  PHPUnit と Laravel の関係を理解した                    │
│      → PHPUnit は PHP のテストツール                       │
│      → Laravel には最初から入ってる                        │
│                                                             │
│  7️⃣  Feature Test と Unit Test の違いを知った               │
│      → Feature Test: API 全体のテスト（今回やった）        │
│      → Unit Test: 部品単体のテスト                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 🎉 今日の成果

**🐘ガネーシャ：** 「お前、今日は**実際にテストを書いて動かした**んやで！」

**👩‍💻ユーザー：** 「はい！緑色で PASS が出た時、すごく嬉しかったです！」

**🐘ガネーシャ：** 「その気持ちを忘れんようにな。テストが通る瞬間は、何回経験してもええもんや」

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│    🎊 おめでとう！                                          │
│                                                             │
│    あなたは今日から                                         │
│    「テストを書けるエンジニア」の仲間入りです！             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**🐘ガネーシャ：** 「まぁ、ワシのおかげやけどな。さすガネーシャや！🐘✨」

**👩‍💻ユーザー：** 「...（そこは素直に受け取っておこう）」

---

### 📚 次回予告

**🐘ガネーシャ：** 「次回は『テストデータの準備』を学ぶで」

**👩‍💻ユーザー：** 「テストデータ？」

**🐘ガネーシャ：** 「せや。今回は認証なしの API やったけど、実際のアプリは**ログインが必要な API**がほとんどやろ？」

**👩‍💻ユーザー：** 「確かに...プロジェクト一覧とか、ログインしないと見れないですよね」

**🐘ガネーシャ：** 「そういう場合、テスト用のユーザーを作らなアカン。そのための仕組みを学ぶで！」

| Lesson | タイトル | 内容 |
|--------|----------|------|
| 7-1 | テストって何？なぜ必要？ | ✅ 完了 |
| 7-2 | PHPUnit と Laravel のテスト基礎 | ✅ **今ここ！** |
| 7-3 | テストデータの準備 | Factory、RefreshDatabase |
| 7-4 | API テスト（正常系） | actingAs、assertStatus |
| 7-5 | API テスト（異常系） | 422、403、404、409 |
| 7-6 | テスト設計の考え方 | 何をテストすべきか |

**🐘ガネーシャ：** 「ほな、次回もよろしくな！さすガネーシャや！🐘✨」
